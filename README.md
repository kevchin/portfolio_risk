## fidelity portfolio risk analysis
  * sample csv from Fidelity -- > sample_holdings.csv
  * sample historic data for a set of holdings and an index (e.g. SPY) -- > sample_history.csv
  * The sample_history can be generated from Google Sheets: =GOOGLEFINANCE(A2,"price",DATE(YEAR(TODAY())-5,1,1),TODAY(),"daily")
